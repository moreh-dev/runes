name: Check skills sync

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check-skills-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Check skills mirror
        run: scripts/check-skills-sync.sh
